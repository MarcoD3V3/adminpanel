/** Altura fija del área lista + ficha (siempre igual en todos los filtros). */
export const PROFILE_WORKSPACE_HEIGHT =
  "h-[36rem] min-h-[36rem] max-h-[36rem] shrink-0";

export const PROFILE_SCROLL =
  "overflow-y-auto overscroll-contain [scrollbar-gutter:stable]";

/** Cuerpo de la ficha — altura mínima fija para que las pestañas no muevan el layout. */
export const PROFILE_DETAIL_BODY_MIN =
  "min-h-[28rem]";

/** Auditoría abajo — altura fija para que no empuje el workspace. */
export const AUDIT_PANEL_HEIGHT =
  "h-[10rem] min-h-[10rem] max-h-[10rem] shrink-0";
