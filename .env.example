# Clave admin + firma digital de layouts del hub (mín. 16 caracteres en producción)
LAUNCHER_ADMIN_SECRET=cambia-esto-por-una-clave-larga-y-secreta

# OBLIGATORIO en Railway/producción — hashea contraseñas y tokens (mín. 16 chars, distinto del admin secret)
# Genera uno: openssl rand -base64 24
LAUNCHER_TOKEN_PEPPER=cambia-esto-pepper-largo-y-aleatorio-min-16

# false = desactiva la exigencia de sesión en /api/hub-builder (solo desarrollo)
LAUNCHER_AUTH_ENFORCE=true

# --- Datos persistentes (hub, perfiles, skins, experimentos…) ---
# Base de datos interna SQLite: data/craftlauncher.db (local y producción).
# En Railway: monta un volumen en /app/data (se usa RAILWAY_VOLUME_MOUNT_PATH automáticamente).
# CRAFTLAUNCHER_DATA_DIR=/app/data
# En dev local: apunta al admin de Railway para que hub/autosave/lock vivan en producción.
# NO definas esto en Railway (ese servidor ES la autoridad).
# HUB_DATA_AUTHORITY_URL=https://tu-app.up.railway.app

# Origen del launcher para CORS (Electron dev usa localhost:1420)
LAUNCHER_ORIGIN=http://localhost:1420

# --- Launcher (Vite / Electron) — leído desde packages/launcher ---
# Admin en producción (prioridad). Pon tu URL de Railway aquí:
# VITE_ADMIN_API_URL=https://tu-app.up.railway.app
# Admin local si producción no responde (npm run dev en otra terminal):
# VITE_ADMIN_API_URL_LOCAL=http://localhost:3000
# false = en dev prioriza Railway (también lo hace npm run launcher:dev:prod)
# VITE_ADMIN_API_PREFER_LOCAL=false

# CurseForge API — token desde https://console.curseforge.com → API Keys
# Suele empezar por $2a$10$ — sin comillas. Reinicia npm run dev y launcher:dev tras cambiarlo.
CURSEFORGE_API_KEY=

# Opcional: token ipinfo.io para geolocalización por IP (https://ipinfo.io/signup)
# Sin token se usa ip-api.com + ipapi.co (gratis, con límites).
# IPINFO_TOKEN=

# --- Player Portal (packages/user_web — segundo servicio en Railway) ---
# ADMIN_API_URL / NEXT_PUBLIC_ADMIN_API_URL → URL pública del admin (servicio 1)
# PORTAL_CLIPBOARD_SECRET → misma clave en admin Y user_web (mín. 16 chars)
# Chat, amigos y mensajes del portal viven en SQLite del admin (volumen /app/data).
# Sesiones (launcher/web) en launcher-auth.json del mismo volumen.
#
# En el servicio ADMIN (opcional si el launcher llama al admin desde el navegador del portal):
# USER_WEB_ORIGIN=https://tu-user-web.up.railway.app
# PORTAL_CLIPBOARD_SECRET=openssl-rand-base64-24
#
# En el servicio USER_WEB (packages/user_web/railway.json):
# ADMIN_API_URL=https://tu-admin.up.railway.app
# NEXT_PUBLIC_ADMIN_API_URL=https://tu-admin.up.railway.app
# PORTAL_CLIPBOARD_SECRET=(la misma que en admin)
