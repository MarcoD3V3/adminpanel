import type { FeaturedModpack } from "../types/launcher-config";

/** Sin modpacks de fábrica — el catálogo lo define solo el admin. */
export const DEFAULT_FEATURED_MODPACKS: FeaturedModpack[] = [];
